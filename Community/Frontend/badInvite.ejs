!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400&display=swap" rel="stylesheet">
    <title>Error</title>
    <style>
        body {
            margin: 0;
        }

        header {
            width: 100%;
            height: 6rem;
            background: linear-gradient(270deg, #FFAA7A 0%, rgba(255, 255, 255, 0) 100%), #F7635B;
            box-shadow: 0px 4px 20px rgba(0, 0, 0, 0.13);
            border-radius: 0px 0px 30px 30px;
        }

        .errHolder {
            max-width: 100%;
            width: 25rem;
            height: 25rem;
            box-shadow: 0px 0px 18px rgba(0, 0, 0, 0.25);
            border-radius: 21px;
            margin: auto;
            margin-top: 7rem;
        }

        .icon {
            position: relative;
            width: 8rem;
            height: auto;
            top: 4rem;
            left: 50%;
            transform: translateX(-50%);
        }

        .err {
            text-align: center;
            margin-top: 7rem;
            font-family: 'Inter', sans-serif;
        }

        .info {
            font-weight: 300;
            font-family: 'Inter', sans-serif;
            color: #8D8D8D;
            font-size: 16px;
            text-align: center;
        }
    </style>
</head>
<body>
    <header>
        
    </header>
    <div class="errHolder">
        <img class="icon" src="owo.svg">
        <h1 class="err">Invalid invite!</h1>
        <p class="info">Please request the admin for another invite!</p>
    </div>
</body>
</html>
