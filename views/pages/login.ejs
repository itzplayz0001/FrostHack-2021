<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Login</title>
    <style>
        body {
            margin: 0;
            height: 100vh;
            background-color: #F5F7F7;
        }
        .field {
            background-color: rgb(252, 252, 252);
            border-radius: 4px;
            border: 2px solid rgb(228, 234, 235);
            box-sizing: border-box;
            display: flex;
            height: 48px;
            outline: none;
            padding-left: 15px;
            padding-right: 15px;
            width: 80%;
            font-family: "Nunito Sans", sans-serif;
            font-weight: 400;
            letter-spacing: normal;
            color: rgb(39, 51, 57);
            font-size: 18px;
            line-height: 27px;
            margin: auto;
        }
        .inputAr {
            position: relative;
            height: max-content;
            background-color: white;
            margin: auto auto;
            border-radius: 10px;
            box-shadow: rgb(0 0 0 / 8%) 0px 1px 4px 0px;
            padding: 40px 20px 50px 20px;
            max-width: 420px;
            top: 50%;
            transform: translateY(-65%);
        }
        .btnn {
            cursor: pointer;
            position: relative;
            left: 50%;
            color: #fff;
            transform: translateX(-50%);
            width: 80%;
            background-color: rgb(55, 112, 255);
            outline: 0;
            border: 0;
            font-size: 20px;
            padding: 14px 10px;
            font-family: 'Inter', sans-serif;
            border-radius: 5px;
        }
        h1 {
            margin: 0;
            font-family: 'Inter', sans-serif;
            color: #1d1d1d;
            text-align: center;
        }
        .err {
            color: #FF0909;
            font-size: 15px;
            text-align: center;
            font-family: 'Inter';
        }
        @media only screen and (max-width: 600px) {
            .email {
                width: 100%;
            }
            .password {
                width: 100%;
            }
        }
    </style>
</head>
<body>
    <% if(success == false){ %>
    <div class="inputAr">
        <h1>Sign In</h1>
        <% if(message != '' || message != 'Successfully logged in!'){ %>
            <p> <%= message %> </p>
        <% } %>
        <input class="field" id="email" type="email" name="email" placeholder="Email Address" required><br>
        <input class="field" id="pass" type="password" name="password" placeholder="Password" required><br>
        <button class="btnn" onclick="submit()">Sign In</button>
    </div>
    <% } %>
    <% if(success == true) { %>
    <div class="inputAr">
        <h1 style="text-align: center;margin-bottom: 16px;">Logged In</h1>
        <button class="btnn" onclick="window.open('http://localhost:8080/', '_self');">Go to home</button>
    </div>
    <% } %>
<script>
    const handleSubmit = (data) => {
        console.log(data)
    }

    function submit(){
        let email = document.querySelector('#email').value;
        let password = document.querySelector('#pass').value;

        window.open(`http://localhost:8080/login?email=${email}&password=${password}`, '_self');

        console.log(email, password);
    }
</script>
</body>
</html>